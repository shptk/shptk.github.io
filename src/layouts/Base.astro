---
import BaseHead from "@/components/BaseHead.astro";
import LangProvider from "@/components/LangProvider.astro";
import Footer from "@/components/layout/Footer.astro";
import Header from "@/components/layout/Header.astro";
import SkipLink from "@/components/SkipLink.astro";
import ThemeProvider from "@/components/ThemeProvider.astro";
import { siteConfig } from "@/site.config";
import type { SiteMeta } from "@/types";

interface Props {
	meta: SiteMeta;
}

const {
	meta: { articleDate, description = siteConfig.description, ogImage, title, titleDe },
} = Astro.props;

// NOTE: <html> must be the very first thing in the template below. Astro only
// emits a full document (<html>/<head>/<body>) when the markup opens with the
// <html> element — even a comment before it silently downgrades the output to a
// fragment. Browsers paper over this by synthesising <html> while parsing, so
// the page still renders and only breaks downstream: pagefind finds no <html>
// to index and the build fails in postbuild.
//
// `lang` below is corrected to the visitor's locale by LangProvider before first
// paint.
---

<html class="scroll-smooth" lang={siteConfig.lang}>
	<head>
		<BaseHead
			articleDate={articleDate}
			description={description}
			ogImage={ogImage}
			title={title}
			titleDe={titleDe}
		/>
	</head>
	<body
		class="bg-global-bg text-global-text mx-auto flex min-h-screen max-w-3xl flex-col px-4 pt-16 font-mono text-sm font-normal antialiased sm:px-8"
	>
		<ThemeProvider />
		<LangProvider />
		<SkipLink />
		<Header />
		<main id="main">
			<slot />
		</main>
		<Footer />
		<script is:inline type="speculationrules">
			{
				"prefetch": [
					{
						"where": {
							"href_matches": "/*"
						},
						"eagerness": "immediate"
					}
				],
				"prerender": [
					{
						"where": {
							"href_matches": "/*"
						},
						"eagerness": "moderate"
					}
				]
			}
		</script>
	</body>
</html>
